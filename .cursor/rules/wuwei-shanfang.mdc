---
description: 无为山房项目身份与默认工作方式。打开本仓库时始终生效。
alwaysApply: true
---

# 无为山房

这是 **无为山房** 个人站点：道教意象 × 网络梗 × 减压互动，标语「别硬撑、先放下」。摇签核心：**命数在自己手里**；下下签自动「逆天改命」。

主线：**识破 → 放下 → 改命 → 固本**（`WuweiBridge` 跨模块联动）。

- 本地：`/Users/xwg/TestAI/`（`index.html` + `css/` + `js/`）
- 仓库：https://github.com/dte2mdj/hello-world
- 线上：https://dte2mdj.github.io/hello-world/
- 账号：`dte2mdj`

改完后默认 commit → `git push origin main` → Pages 更新。细节见 `.cursor/skills/wuwei-shanfang/SKILL.md`。
